{
  "name": "shogun-ai-hifi",
  "private": true,
  "version": "0.4.1",
  "license": "UNLICENSED",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "check:actions": "python3 hifi/scripts/check-actions.py",
    "check:ipc-mock": "node hifi/scripts/check-ipc-mock-sync.mjs",
    "check:rust": "cargo check --manifest-path src-tauri/Cargo.toml",
    "test:rust": "cargo test --manifest-path src-tauri/Cargo.toml --locked",
    "build:web-dist": "node scripts/sync-web-dist.mjs",
    "build:icons": "node scripts/build-icons.mjs",
    "tauri": "tauri",
    "dev:desktop": "tauri dev",
    "build:desktop": "tauri build",
    "build:desktop:signed": "tauri build -c src-tauri/tauri.signing.local.json"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "@tauri-apps/cli": "^2"
  }
}
